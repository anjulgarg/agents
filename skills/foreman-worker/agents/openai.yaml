interface:
  display_name: "Foreman Worker"
  short_description: "Run an ephemeral or persistent local coding harness"
  default_prompt: "Use $foreman-worker to invoke the requested coding harness with the required session lifecycle, then independently verify the result."
policy:
  allow_implicit_invocation: false
