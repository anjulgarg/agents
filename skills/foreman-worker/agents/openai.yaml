interface:
  display_name: "Foreman Worker"
  short_description: "Run a native or CLI worker with a stable lifecycle"
  default_prompt: "Use $foreman-worker to select a native resumable-subagent or CLI transport, preserve the requested session lifecycle, and independently verify the result."
policy:
  allow_implicit_invocation: false
