# Provider metadata

Use a provider reference only when Git alone cannot resolve immutable base and head snapshots.

- [GitHub](github.md)
- [Azure DevOps](azure-devops.md)

For other providers, prefer user-supplied refs or SHAs and the Git-first procedure in `../git-scope.md`. Add a reviewed provider reference only when provider metadata materially improves correctness or usability.
