# Local Pi configuration

This directory contains Pi-specific configuration. `settings.json`, `models.json`, and `mcp.json` are machine- and account-specific, so they remain local and are ignored by Git and npm packaging.

The tracked `keybindings.json` file contains the public keyboard bindings used by the Pi resources.
