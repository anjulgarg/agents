# Local Pi configuration

This directory contains optional Pi-specific configuration. `models.json` and `mcp.json` are machine- and account-specific, so they remain local and are ignored by Git and npm packaging. Pi settings remain user-owned and are never installed by this repository.

The tracked `keybindings.json` file contains the public keyboard bindings used by the Pi resources.
