# Local Pi configuration

This directory contains optional Pi-specific configuration. `mcp.json` is machine- and account-specific, so it remains local and is ignored by Git and npm packaging. Pi settings remain user-owned and are never installed by this repository.

The tracked `keybindings.json` file contains the public keyboard bindings used by the Pi resources.
