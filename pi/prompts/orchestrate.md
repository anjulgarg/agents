---
description: Orchestrate work through delegation
argument-hint: "<agent-or-harness>"
---

Orchestrate this task using $@; parallelize independent work. Delegates see only your task text, so put each complete delegation brief directly in `task` (objective, exclusive file ownership, constraints, deliverables, verification). Never create a prompt or system-prompt file merely for a native subagent to read. Cite existing source artifacts by exact path and name, absolute for ignored or untracked files (e.g., plan documents); embed content when cheaper; never rely on discovery. You remain accountable for correctness, quality, completeness, and verification.
